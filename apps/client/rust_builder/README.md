# ContinueHere Bridge

This package integrates the ContinueHere Rust bridge with Flutter's native
builds. Its platform files are maintained alongside the generated bridge
bindings and the bundled Cargokit build support.
