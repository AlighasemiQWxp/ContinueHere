import { FilledButton, MaterialPalette, OutlineButton, Slider, TextButton, TextField, TonalButton } from "@material";
import { AppTheme } from "theme.slint";

export component AppSlider inherits Slider {
    callback changed(float);
    value-changed(value) => { root.changed(value); }
}

export component AppTextField inherits TextField {
    min-height: 48px;
}

export component Button inherits FilledButton {
    enabled: true;
    min-height: 40px;
}

export component SecondaryButton inherits OutlineButton {
    enabled: true;
    min-height: 40px;
}

export component QuietButton inherits TextButton {
    enabled: true;
    min-height: 36px;
}

export component ContentButton inherits TonalButton {
    enabled: true;
    min-height: 64px;
}

export component Title inherits Text {
    in property <bool> rtl;
    color: MaterialPalette.on-surface;
    font-size: 24px;
    font-weight: 700;
    horizontal-alignment: rtl ? right : left;
    vertical-alignment: center;
    wrap: char-wrap;
}

export component Label inherits Text {
    in property <bool> rtl;
    color: MaterialPalette.on-surface-variant;
    font-size: 14px;
    horizontal-alignment: rtl ? right : left;
    vertical-alignment: center;
    wrap: char-wrap;
}

export component Card inherits Rectangle {
    background: AppTheme.card;
    border-color: MaterialPalette.outline-variant;
    border-width: 1px;
    border-radius: 14px;
    drop-shadow-blur: 10px;
    drop-shadow-color: #00000020;
    drop-shadow-offset-y: 3px;
}

export component Badge inherits Rectangle {
    in property <bool> shown;
    width: 9px;
    height: 9px;
    border-radius: 5px;
    background: #ffd16a;
    opacity: shown ? 1 : 0;
    animate opacity { duration: 150ms; }
    accessible-role: text;
    accessible-label: "New activity";
}
